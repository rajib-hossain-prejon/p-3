import { createContext } from "react";

export  const InfoData = createContext([]);
export  const ServicesData = createContext([]);
export  const HomepageData = createContext([]);
export  const ProjectsData = createContext([]);
export  const AuthContext = createContext({});
